# sinigrind

////Orcio, John Michael P.
I figured first there's something wrong with our sql or database, and not actually with the code. I have been scrolling for the php codes for almost an hour, searching for something I don't even know what.
Right after running the debugging activity—the bugged sinigrind system—when the warning/alert showed up, it says that there is a problem creating an order. But then the first checkout actually pushed through, but the alert that I created was supposed to alert what order is created/checked out, says order id: 0 successful, I didn’t think so much about that at first, I just thought hhm, that’s sus –haha eme eme talaga. The second time I’m about to check out, didn’t push through. So, my first thought, most probably, what causes the error lies in the checkout page. I went through the checkout page, and nothing seems wrong with it. We’ve also searched for bugs using the DevTool/Inspect on the browser, but it doesn’t show any errors. Then again I went through the PHP code that could have even the least influence on ordering, but found nothing. That seems like forever then, fortunately, I went through our database, singrind_data.sql, went through each table, then my eyes caught the AUTO_INCREMENT tag on the order table’s missing. I am sure that that is supposed to be A_I because it’s a primary key. But it’s so funny what I did next—I am just so excited, I saw that nuance in the database—I truncated the table's content, entirely. I know that’s not the right way to do it, but I did it because of excitement. Also, what’s just on my mind at that time is to debug the system, whatever the process could be. Basically, what I’m supposed to achieve is that I have to modify the order id/primary key into AUTO_INCREMENT, but at first, there are still a lot of errors that we’ve encountered. Moving forward, we were able to set the order id as A_I, again, referenced that again as a foreign key on the order_items table. Everything’s good, the relationship between the order and order_items was restored, then proceeded to check the system if it’s working again, it is.
Despite all that, we were still unable to find the line in our code that triggered the change on the database. But only after all that, we knew we had to find a line in the code that says ALTER, MODIFY order table. We went through the php codes again, now with more confidence in what we were trying to find, and in the login.php, we found the bug. From lines 104 to 107 lies the encrypted bug, that actually says what the encrypted bug is for. 
